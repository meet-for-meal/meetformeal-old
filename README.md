# Meetformeal

## Requirements

* ruby 2.1.0
* rails 4.0.3

## Install

Create `config/application.yml` from `config/application.example.yml`. Then run:

    bundle install
    rake db:crate
    rake db:migrate
    rake db:prepare

## Launch

    rails s

## Test

`@TODO`
